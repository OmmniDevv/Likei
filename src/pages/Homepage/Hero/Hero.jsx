import React from "react";
import styles from "./HeroStyles.module.css";

const Hero = () => {
  return (
    <section className={styles.hero}>
      <h1>Hero</h1>
    </section>
  );
};

export default Hero;
